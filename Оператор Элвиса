//Оператор Эльвиса позволяет предоставить значение по умолчанию, если переменная равна `null`:
   
    fun main() {
    
//В данной строке используется оператор безопасного вызова `?.` для получения длины строки. 
//Если `name` не равна `null`, то `name?.length` возвращает длину строки. В противном случае возвращается `null`. 
//Оператор Эльвиса `?:` обеспечивает возврат значения по умолчанию (в данном случае `0`), 
//если значение `null`.
    var name: String? = "Джон"
    var length = name?.length ?: 0
    println(length) // печатает 4

    name = null
    length = name?.length ?: 0
    println(length) // печатает 0
}
