//В Kotlin переменные, объявленные внутри метода, называются локальными и существуют только в пределах этого метода. 
//Свойства класса, объявленные вне методов, доступны для экземпляров этого класса и могут иметь различные модификаторы доступа.

class Example {
    var property: Int = 0  // Свойство класса

    fun method() {
        val localVariable: Int = 10  // Локальная переменная
        println(localVariable)
    }
}

